apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: applications
  labels:
    app: nginx
    environment: lab
spec:
  containers:
    - name: nginx
      image: nginx:1.28
      ports:
        - containerPort: 80
